#include<iostream>

using namespace std;

int main()
{
    float sc, n, avg;
    cout<<"Enter the score : ";
    cin>>sc;
    cout<<"Enter which times out : ";
    cin>>n;
    avg = sc/n;
    cout<<"Batting Average : "<<avg;
    
    return 0;
    
}
